import { UsersRound } from 'lucide-react';
import { ComingSoon } from '@/components/admin/ComingSoon';

export default function ParticipantsPage() {
  return (
    <ComingSoon
      title="Participants"
      icon={UsersRound}
      note="Liste, filtres et fiches participants — livrés au Lot 8."
    />
  );
}
